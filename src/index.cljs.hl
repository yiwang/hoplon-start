(page "index.html")

(defc things ["one" "two" "three"])

(html
  (head
    (link :href "css/bootstrap.css" :rel "stylesheet"))
  (body
    (ul
      (loop-tpl :bindings [x things]
        (li x)))))
